using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sorter : MonoBehaviour
{
    // column.transform.position.y = 0.42
    // column.transform.scale.y = 0.16
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
